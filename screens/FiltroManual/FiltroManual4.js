import { SafeAreaView, StyleSheet, Text } from "react-native";


const FiltroManual4 = () => {
    return (
        <SafeAreaView style={styles.container}>
            <Text>FiltroManual4</Text>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center'
    }
});

export default FiltroManual4;