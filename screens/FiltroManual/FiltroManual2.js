import { SafeAreaView, StyleSheet, Text } from "react-native";


const FiltroManual2 = () => {
    return (
        <SafeAreaView style={styles.container}>
            <Text>FiltroManual2</Text>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center'
    }
});

export default FiltroManual2;